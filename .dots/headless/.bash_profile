#!/bin/bash
# ~/.bash_profile defines settings for users when running the login shell.

[[ -f ~/.bashrc.profile ]] && source ~/.bashrc.profile
[[ -f ~/.bashrc.unmanaged ]] && source ~/.bashrc.unmanaged
[[ -f ~/.bashrc ]] && source ~/.bashrc

